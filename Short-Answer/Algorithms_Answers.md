#### Please add your answers to the ***Analysis of  Algorithms*** exercises here.

## Exercise I

a) O(n) --> The runtime will grow at the same rate for each input and iterates a until the condition is met at n^3.


b) O(n log n) --> The runtime is decreasing as j increases by 2 where the algorithm loops through each of the inputs. 


c) O(n) --> The runtime is linear with any size of bunnies. Each time the function is called it increases by 2.

## Exercise II

II) O(log n) --> The runtime using a binary search to guess floor f with the floors in order, will start the search in the middle and parse through half of the floors. 
